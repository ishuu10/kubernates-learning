fixed set ,not type stafe

java.util.Arrays


int[] a={1,2,4}
int[] a=new int[5]

a.length -> 3
a.toString()-> #[1, 2, 4]
a.sort()

a[3]=5 # a->{1,2,4,5}

for (int i:a)
{
    system.out.println(i)
}

for (int i=0;i<a.length;i++)
{
    system.out.println(a[0])
}

int[] b=new int[5]

for (int i=0;i<a.length;i++)
{
    b[i]=i
}

cannot use add directly on arrays 

