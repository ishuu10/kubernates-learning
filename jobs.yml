apiVersion: batch/v1
kind: Job

metadata:
  name: kubernates-learning 

spec:
  template:
    spec:
      containers:
      - name: playwright
        image: ishuu10/playwright-demo-todo:v1
        imagePullPolicy: Always

      restartPolicy: Never